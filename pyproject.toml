[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-notes-rag-project"
version = "0.1.0"
description = "Personal Notes RAG Project with Qdrant dense retrieval, Chinese BM25, intent routing, and evidence-grounded answers."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Codex" }]
dependencies = [
  "fastapi>=0.110",
  "httpx>=0.27",
  "numpy>=1.26",
  "bm25-jieba==0.2.2",
  "pydantic>=2.7",
  "pydantic-settings>=2.2",
  "python-dotenv>=1.0",
  "pyyaml>=6.0",
  "qdrant-client[fastembed]>=1.10",
  "typer>=0.12",
  "uvicorn>=0.29",
]

[project.optional-dependencies]
models = [
  "sentence-transformers>=3.0",
]
dev = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.5",
]

[project.scripts]
personal-notes-rag = "legal_rag.cli:app"
legal-rag = "legal_rag.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
legal_rag = ["web/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
